<swiper-item
  id="{{id}}"
  class="{{className}}"
  style="{{style}}"
  data-goji-id="{{gojiId}}"
  item-id="{{itemId}}"
>
  <import src="../components0.wxml" />
  <block wx:for="{{nodes}}" wx:key="id">
    <template is="$$GOJI_COMPONENT0" data="{{ ...item }}" />
  </block>
</swiper-item>
